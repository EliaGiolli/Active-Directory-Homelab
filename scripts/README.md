# Scripts

PowerShell automation will land here as the project matures — the plan is to first do each task manually through the GUI to understand what's happening under the hood, then revisit it as a script once the concept is solid.

Planned scripts:
- Bulk OU / user / group creation from a CSV
- GPO backup / import helpers
- Environment health-check script (services, replication, DNS records)
